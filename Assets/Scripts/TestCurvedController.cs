using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestCurvedController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Update()
    {
        //CurvedUIInputModule.CustomControllerRay = new Ray(transform.position, transform.forward);

        //CurvedUIInputModule.CustomControllerButtonState = Input.GetKeyDown(KeyCode.A);
    }

}
